apiVersion: v1
kind: Service
metadata:
    name: shmohantydotnetcoredockersamples
spec:
    type: LoadBalancer
    ports:
    - port: 80 
    selector:
        app: shmohantydotnetcoredockersamples